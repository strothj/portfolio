{
  "name": "app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "components": "1.0.0",
    "react": "^16.3.0",
    "react-dom": "^16.3.0",
    "react-scripts": "2.0.0-next.b2fd8db8"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "npm-run-all clean build:*",
    "build:webpack": "react-app-rewired build",
    "build:rename": "shx mv build dist",
    "clean": "rimraf build dist",
    "test": "react-app-rewired test --env=jsdom"
  },
  "browserslist": {
    "development": [
      "last 2 chrome versions",
      "last 2 firefox versions",
      "last 2 edge versions"
    ],
    "production": [
      ">1%",
      "last 4 versions",
      "Firefox ESR",
      "not ie < 11"
    ]
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.0.0-beta.42",
    "@types/jest": "^22.2.2",
    "@types/react": "^16.1.0",
    "@types/react-dom": "^16.0.4",
    "npm-run-all": "^4.1.2",
    "react-app-rewire-typescript-babel-preset": "^2.1.0",
    "react-app-rewired": "^2.0.0",
    "rimraf": "^2.6.2",
    "shx": "^0.2.2"
  }
}
